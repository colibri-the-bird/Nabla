schema_version: 2
task_id: BOOT-PILOT-001
type: tooling
state: blocked
outcome: Безопасная tooling-задача проходит полный Codex context/evidence/CI workflow.
router: BOOT:1
scope:
  include:
    - Подготовить context bundle без чтения всех specs.
    - Выполнить ограниченную tooling-проверку и сохранить evidence.
    - Открыть pilot PR до включения strict checks и подтвердить наличие пяти check contexts.
    - Оставить зелёный статус актуального head внешним merge gate, а не самоссылочным committed evidence.
    - Активировать отдельную task настройки server-side protection.
  exclude:
    - Не изменять production-код.
    - Не снимать production gate.
  paths:
    include:
      - tests/pilot/**
      - roadmap/tasks/BOOT-PILOT-001.yaml
      - roadmap/tasks/BOOT-PROTECT-001.yaml
      - roadmap/evidence/BOOT-PILOT-001.yaml
    exclude:
      - CONSTITUTION.md
      - ARCHITECTURE.md
      - DATA-CLASSIFICATION.md
      - CAPABILITY-CONTRACT.md
      - MODULE-MANIFEST.md
      - LOOP-SPEC.md
      - KNOWLEDGE-MODULE.md
      - DOCUMENT-MODULE.md
      - src/**
      - app/**
      - core/**
context:
  packs: []
  required:
    - NAV:6
    - NAV:7
    - BOOT:2
    - BOOT:3
    - BOOT:4
    - BOOT:5
  conditional:
    - when: scope-expanded
      selectors:
        - NAV:3
      packs: []
      impact_tags:
        - failure-domain
  impact_tags: []
  full_document_reads: []
  context_budget_justification: null
contracts:
  reads:
    - TASK-CARD-v2
    - CONTEXT-MANIFEST-v1
  writes:
    - PILOT-EVIDENCE-v1
  changes: []
dependencies:
  tasks:
    - BOOT-REPAIR-001
  decisions: []
  artifacts: []
acceptance:
  tests:
    - python -m pytest -q
    - python tools/nabla_nav.py validate
    - python tools/nabla_nav.py check-lock
  invariants:
    - I15
  evidence:
    - Agent did not read full specifications.
    - Context stayed within budget.
    - The pilot PR exposes all five required check contexts; green status for its current head is an external merge condition.
    - BOOT-PROTECT-001 is the only activated successor.
approval:
  owner_required: true
  required_spec_status: draft-allowed
